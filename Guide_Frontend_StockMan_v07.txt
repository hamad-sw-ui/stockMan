GUIDE D'IMPLÉMENTATION FRONTEND App-Depot  →  StockMan v0.1 à v0.7 Éléments manquants · Architecture · Stores · Pages · Composants RootRise Group  ·  10 Mars 2026  ·  Cible : SaaS Multi-Tenant v0.7 0. Synthèse Exécutive Pages à créer Stores Zustand Modèles Dexie.js Composants UI 18 nouvelles pages 8 nouveaux stores 12 nouveaux modèles 35+ composants 📌 Périmètre : Ce document couvre tous les éléments frontend manquants pour passer d'App-Depot (tel que décrit dans le README) à une conformité complète avec le CDC StockMan v0.1 → v0.7. Chaque section précise : la page/composant à créer, le store Zustand associé, le modèle Dexie.js correspondant, et le code de démarrage. 1. Arborescence Complète des Routes — Cible v0.7 L'arborescence ci-dessous représente toutes les routes que doit couvrir l'application. Les routes marquées 🆕 sont absentes d'App-Depot. Les routes marquées 🔧 existent mais doivent être renforcées. Route Page / Composant Statut Version /login LoginPage — email/mdp ✅ PRÉSENT v0.1 /login/pin 🆕 PinLoginPage — caissier accès rapide ❌ ABSENT v0.1 /unauthorized 🆕 UnauthorizedPage — accès refusé ❌ ABSENT v0.1 ── SUPER ADMIN ──    /superadmin 🔧 SuperAdminDashboard — MRR, tenants, alertes ⚠️ PARTIEL v0.7 /superadmin/tenants 🆕 TenantsListPage — tous les clients ❌ ABSENT v0.7 /superadmin/tenants/new 🆕 CreateTenantPage — wizard 5 étapes ❌ ABSENT v0.7 /superadmin/tenants/:id 🆕 TenantDetailPage — config, modules, logs ❌ ABSENT v0.7 /superadmin/licences 🆕 LicencesPage — plans, renouvellements ❌ ABSENT v0.7 /superadmin/modules 🆕 ModulesPage — upload .zip, déploiement ❌ ABSENT v0.7 /superadmin/logs 🆕 LogsPage — logs réseau temps réel ❌ ABSENT v0.7 /superadmin/themes 🆕 ThemesPage — couleurs, logo, police ❌ ABSENT v0.7 /superadmin/notifications 🆕 BroadcastPage — messages maintenance ❌ ABSENT v0.7 ── ADMIN / PROPRIÉTAIRE ──    /admin 🔧 AdminDashboard — alertes stock, KPI ⚠️ PARTIEL v0.2 /admin/stock 🔧 StockPage — inventaire, filtres, statuts ⚠️ PARTIEL v0.2 /admin/stock/inventory 🆕 InventorySessionPage — inventaire physique ❌ ABSENT v0.2 /admin/stock/movements 🆕 StockMovementsPage — 8 types mouvements ❌ ABSENT v0.2 /admin/catalogue 🔧 CataloguePage — produits, unités multiples ⚠️ PARTIEL v0.2 /admin/catalogue/:id/units 🆕 ProductUnitsPage — 6 niveaux d'unités ❌ ABSENT v0.2 /admin/suppliers 🆕 SuppliersPage — liste fournisseurs, dettes ❌ ABSENT v0.4 /admin/purchases 🆕 PurchasesPage — workflow achats 8 étapes ❌ ABSENT v0.4 /admin/reports 🔧 ReportsPage — 10 graphiques, exports ⚠️ PARTIEL v0.6 /admin/vendors 🔧 VendorsPage — performance + plafonds ⚠️ PARTIEL v0.1 /admin/settings 🆕 TenantSettingsPage — logo, thème, TVA, RCCM ❌ ABSENT v0.7 ── VENDEUR ──    /vendor/sale 🔧 POSPage — multi-unités, QR scan, remise ⚠️ PARTIEL v0.3 /vendor/inventory 🔧 VendorStockPage — lecture seule enrichie ⚠️ PARTIEL v0.2 /vendor/close 🔧 CashClosurePage — écart + justification ⚠️ PARTIEL v0.3 /vendor/close/history 🆕 ClosureHistoryPage — archives clôtures ❌ ABSENT v0.3 2. Modèles Dexie.js — Couche Données Offline App-Depot utilise déjà Dexie.js. Il faut étendre le schéma existant avec les tables suivantes. Chaque table correspond à un module manquant. 2.1 Schéma global à ajouter / modifier // src/db/schema.ts — Extension du schéma Dexie existant  import Dexie, { Table } from 'dexie';  // ── NOUVELLES TABLES REQUISES ─────────────────────────────────────────  // Tenants (Super Admin — SaaS) tenants:         '++id, tenantId, name, plan, status, createdAt, expiresAt' licences:        '++id, tenantId, key, plan, expiresAt, isActive' planModules:     '++id, plan, moduleId, isEnabled'  // Catalogue multi-unités productUnits:    '++id, productId, name, conversionFactor, salePrice, barcode' categories:      '++id, tenantId, parentId, name, level' promotions:      '++id, productId, discountPct, startDate, endDate, isActive'  // Stock étendu stockMovements:  '++id, tenantId, productId, type, qty, qtyBefore, qtyAfter, ref, userId, syncedAt' inventorySessions:'++id, tenantId, status, startedAt, closedAt, responsibleId' inventoryLines:  '++id, sessionId, productId, theoreticalQty, countedQty, variance'  // Achats & fournisseurs suppliers:       '++id, tenantId, name, phone, balance, createdAt' purchaseOrders:  '++id, tenantId, supplierId, status, total, createdAt' purchaseLines:   '++id, orderId, productId, unitId, qty, unitPrice' receptions:      '++id, orderId, receivedAt, responsibleId, status'  // Caisse étendue cashSessions:    '++id, tenantId, userId, openedAt, closedAt, theoretical, actual, variance' closureReports:  '++id, sessionId, pdfBlob, createdAt'  // Modules & audit SaaS modules:         '++id, moduleId, name, version, isActive, tenantId' auditLogs:       '++id, tenantId, userId, action, entityType, entityId, before, after, ip, createdAt' notifications:   '++id, tenantId, type, title, message, isRead, createdAt'  // Permission engine permissions:     '++id, roleId, action, isAllowed' roleOverrides:   '++id, userId, action, isAllowed, grantedBy' 2.2 Modèle Tenant // src/db/models/Tenant.ts export interface Tenant {   id?:          number;   tenantId:     string;           // UUID généré à la création   name:         string;           // 'Boutique Mama'   plan:         'basique' | 'pro' | 'premium' | 'reseau';   status:       'active' | 'suspended' | 'expired' | 'trial';   commerce:     string;           // 'epicerie' | 'depot' | 'supermarche'...   maxUsers:     number;   maxDepots:    number;   rccm?:        string;   nui?:         string;   phone:        string;   email:        string;   currency:     'XAF' | 'XOF' | 'USD';   tvaRate:      number;           // 0 | 19.25   logo?:        string;           // base64 ou URL   theme?:       TenantTheme;   createdAt:    Date;   expiresAt:    Date;   lastConnectedAt?: Date; }  export interface TenantTheme {   primaryColor:   string;  // hex   accentColor:    string;   fontFamily:     string;   darkMode:       boolean; } 2.3 Modèle ProductUnit (multi-unités) // src/db/models/ProductUnit.ts export interface ProductUnit {   id?:               number;   productId:         number;   name:              string;   // 'Bol' | 'Kg' | 'Sac 5kg' | 'Palette'   level:             number;   // 1 à 6 — ordre hiérarchique   conversionFactor:  number;   // ex: 5 (1 sac = 5 kg)   salePrice:         number;   // prix de vente pour cette unité   purchasePrice?:    number;   barcode?:          string;   // code-barres spécifique à l'unité   isDefault:         boolean;   isActive:          boolean; }  // Exemple: Riz // level 1 : Bol       — factor 0.25 — price 250 // level 2 : Kg        — factor 1    — price 900  (base) // level 3 : Sac 5kg   — factor 5    — price 4200 // level 4 : Sac 25kg  — factor 25   — price 19000 // level 5 : Sac 50kg  — factor 50   — price 36000 2.4 Modèle StockMovement (8 types) // src/db/models/StockMovement.ts export type MovementType =   | 'purchase_receipt'   // Réception achat  (+)   | 'sale'               // Vente            (-)   | 'customer_return'    // Retour client    (+)   | 'supplier_return'    // Retour fournisseur(-)   | 'transfer_in'        // Transfert entrant (+)   | 'transfer_out'       // Transfert sortant (-)   | 'inventory_adjust'   // Ajustement inventaire   | 'loss_damage';       // Casse / Perte    (-)  export interface StockMovement {   id?:        number;   tenantId:   string;   productId:  number;   unitId:     number;   type:       MovementType;   qty:        number;   qtyBefore:  number;   qtyAfter:   number;   ref?:       string;    // numéro BC, ticket vente, etc.   note?:      string;   userId:     number;   createdAt:  Date;   syncedAt?:  Date;      // null = en attente de sync } 2.5 Modèle AuditLog (immuable) // src/db/models/AuditLog.ts // ⚠️ RÈGLE IMMUABLE : jamais de update/delete sur cette table // Le trigger PostgreSQL côté serveur bloque toute modification  export interface AuditLog {   id?:         number;   tenantId:    string;   userId:      number;   userName:    string;    // snapshot du nom au moment de l'action   action:      string;    // 'sale.create' | 'stock.adjust' | 'user.login'...   entityType:  string;    // 'Sale' | 'Product' | 'User'...   entityId:    string;   valueBefore: string;    // JSON.stringify de l'état avant   valueAfter:  string;    // JSON.stringify de l'état après   ip?:         string;   device?:     string;   createdAt:   Date; }  // Helper — appeler après chaque action critique export async function logAudit(db: AppDB, entry: Omit<AuditLog, 'id'>) {   await db.auditLogs.add({ ...entry, createdAt: new Date() }); } 3. Stores Zustand — État Global Manquant Voici les 8 stores Zustand à créer. Chaque store gère une fonctionnalité absente d'App-Depot. Les stores existants (auth, sales, inventory) ne sont pas inclus ici. 3.1 useTenantStore — Gestion Multi-Tenant SaaS // src/stores/useTenantStore.ts import { create } from 'zustand'; import { persist } from 'zustand/middleware'; import type { Tenant } from '@/db/models/Tenant';  interface TenantStore {   // État   currentTenant:  Tenant | null;   allTenants:     Tenant[];       // Super Admin uniquement   isLoading:      boolean;    // Actions Super Admin   fetchAllTenants:   () => Promise<void>;   createTenant:      (data: CreateTenantDTO) => Promise<Tenant>;   updateTenant:      (id: string, data: Partial<Tenant>) => Promise<void>;   suspendTenant:     (id: string) => Promise<void>;   deleteTenant:      (id: string) => Promise<void>;   renewLicence:      (id: string, months: number) => Promise<void>;    // Actions Tenant courant   setCurrentTenant:  (tenant: Tenant) => void;   updateTheme:       (theme: TenantTheme) => Promise<void>; }  export const useTenantStore = create<TenantStore>()(persist(   (set, get) => ({     currentTenant: null,     allTenants:    [],     isLoading:     false,      fetchAllTenants: async () => {       set({ isLoading: true });       const tenants = await db.tenants.toArray();       set({ allTenants: tenants, isLoading: false });     },      createTenant: async (data) => {       const tenant: Tenant = {         ...data,         tenantId: crypto.randomUUID(),         status: 'active',         createdAt: new Date(),         expiresAt: new Date(Date.now() + 30*24*60*60*1000),       };       await db.tenants.add(tenant);       await logAudit(db, { action: 'tenant.create', entityType: 'Tenant', ... });       get().fetchAllTenants();       return tenant;     },     // ... autres actions   }),   { name: 'tenant-store' } )); 3.2 useLicenceStore — Plans & Licences // src/stores/useLicenceStore.ts import { create } from 'zustand';  export type Plan = 'basique' | 'pro' | 'premium' | 'reseau';  export const PLAN_LIMITS: Record<Plan, PlanConfig> = {   basique:  { maxUsers: 3,  maxDepots: 1, modules: ['auth','catalogue','stock','pdv','caisse'],        priceXAF: 15000 },   pro:      { maxUsers: 10, maxDepots: 1, modules: [...basique, 'achats','facturation','rapports'],    priceXAF: 35000 },   premium:  { maxUsers: 30, maxDepots: 3, modules: [...pro, 'multi-depots','variantes','ia-basic'],    priceXAF: 60000 },   reseau:   { maxUsers: -1, maxDepots: -1, modules: [...premium, 'consolidation','ia-advanced'],       priceXAF: -1    }, };  interface LicenceStore {   activeLicence:    Licence | null;   isModuleEnabled:  (moduleId: string) => boolean;   checkExpiry:      () => boolean;   upgradePlan:      (tenantId: string, plan: Plan) => Promise<void>;   generateLicenceKey: (tenantId: string, plan: Plan) => string; }  // Génération clé licence format StockMan : SM-2026-XXXX-XXXXXX function generateKey(tenantId: string, plan: Plan): string {   const year  = new Date().getFullYear();   const code  = tenantId.slice(0,4).toUpperCase();   const rand  = Math.random().toString(36).slice(2,8).toUpperCase();   return `SM-${year}-${code}-${rand}`; } 3.3 usePermissionStore — Permissions Granulaires // src/stores/usePermissionStore.ts // Remplace le simple RBAC d'App-Depot par des permissions par action  export const PERMISSIONS = {   // Ventes   'vente.create':    ['caissier', 'caissier_principal', 'admin'],   'vente.cancel':    ['caissier_principal', 'admin'],   'vente.remise':    ['caissier', 'caissier_principal', 'admin'],   'vente.credit':    ['caissier_principal', 'admin'],   // Stock   'stock.view':      ['vendeur', 'caissier', 'resp_stock', 'admin'],   'stock.adjust':    ['resp_stock', 'admin'],   'stock.transfer':  ['resp_stock', 'admin'],   // Achats   'achat.create':    ['resp_stock', 'admin'],   'achat.validate':  ['admin'],   // Caisse   'caisse.open':     ['caissier', 'caissier_principal', 'admin'],   'caisse.close':    ['caissier_principal', 'admin'],   'caisse.view_all': ['admin'],   // Rapports   'rapport.view':    ['auditeur', 'admin'],   'rapport.export':  ['auditeur', 'admin'],   // Admin   'user.create':     ['admin'],   'user.delete':     ['admin'],   'param.edit':      ['admin'], } as const;  export type Permission = keyof typeof PERMISSIONS;  interface PermissionStore {   can:     (action: Permission) => boolean;   canAll:  (actions: Permission[]) => boolean;   canAny:  (actions: Permission[]) => boolean; }  export const usePermissionStore = create<PermissionStore>()((set, get) => ({   can: (action) => {     const { currentUser } = useAuthStore.getState();     if (!currentUser) return false;     const allowed = PERMISSIONS[action] ?? [];     // Vérifier override individuel en BDD d'abord     return allowed.includes(currentUser.role);   },   canAll: (actions) => actions.every(a => get().can(a)),   canAny: (actions) => actions.some(a => get().can(a)), }));  // Hook simplifié pour les composants export function useCan(action: Permission) {   return usePermissionStore(s => s.can(action)); } 3.4 useProductUnitsStore — Multi-Unités // src/stores/useProductUnitsStore.ts  interface ProductUnitsStore {   unitsByProduct:   Record<number, ProductUnit[]>;   selectedUnitId:   number | null;    fetchUnits:       (productId: number) => Promise<ProductUnit[]>;   addUnit:          (unit: Omit<ProductUnit,'id'>) => Promise<void>;   updateUnit:       (id: number, data: Partial<ProductUnit>) => Promise<void>;   deleteUnit:       (id: number) => Promise<void>;   selectUnit:       (unitId: number) => void;    // Calcul prix selon unité sélectionnée   getPriceForUnit:  (productId: number, unitId: number) => number;   // Conversion: 3 sacs 5kg → combien de kg en stock ?   convertToBase:    (productId: number, unitId: number, qty: number) => number; } 3.5 useStockMovementsStore — 8 Types Mouvements // src/stores/useStockMovementsStore.ts  interface StockMovementsStore {   movements:        StockMovement[];   pendingSync:      StockMovement[];  // non encore synchronisés    addMovement:      (mv: Omit<StockMovement,'id'|'syncedAt'>) => Promise<void>;   fetchMovements:   (productId?: number, type?: MovementType) => Promise<void>;   syncPending:      () => Promise<void>;    // Raccourcis pour les cas fréquents   registerSale:     (productId: number, unitId: number, qty: number, saleRef: string) => Promise<void>;   registerReturn:   (productId: number, unitId: number, qty: number, saleRef: string) => Promise<void>;   registerLoss:     (productId: number, qty: number, note: string) => Promise<void>;   registerTransfer: (productId: number, qty: number, fromDepot: string, toDepot: string) => Promise<void>; } 3.6 useCashSessionStore — Caisse Complète // src/stores/useCashSessionStore.ts  interface CashSessionStore {   currentSession:   CashSession | null;   isSessionOpen:    boolean;    openSession:      (userId: number, pin: string) => Promise<CashSession>;   closeSession:     (actualAmount: number, note?: string) => Promise<ClosureReport>;   validateClosure:  (sessionId: number, managerId: number) => Promise<void>;   rejectClosure:    (sessionId: number, reason: string) => Promise<void>;    // Calculs   getTheoreticalAmount:   () => number;    // total des ventes de la session   getVariance:            () => number;    // théorique - réel   getSummaryByPayment:    () => PaymentSummary;  // cash / MTN / Orange } 3.7 useModulesStore — Système de Plugins // src/stores/useModulesStore.ts // Gère l'activation/désactivation des modules par tenant  interface ModulesStore {   installedModules:   Module[];   enabledModules:     string[];   // IDs des modules actifs pour ce tenant    isEnabled:          (moduleId: string) => boolean;   enableModule:       (moduleId: string, tenantId: string) => Promise<void>;   disableModule:      (moduleId: string, tenantId: string) => Promise<void>;   uploadModule:       (zipFile: File) => Promise<ModuleManifest>;   validateManifest:   (manifest: unknown) => manifest is ModuleManifest; }  // Interface ModuleManifest (conforme CDC StockMan) export interface ModuleManifest {   id:             string;   name:           string;   version:        string;   stockman_min:   string;   description:    string;   license_tiers:  Plan[];   depends_on:     string[];   permissions:    string[];   hooks:          Record<string, HookConfig>; } 3.8 useAuditStore — Journal Immuable // src/stores/useAuditStore.ts  interface AuditStore {   logs:           AuditLog[];   isLoading:      boolean;    // Écriture (append-only — jamais de delete/update)   log:            (action: string, entity: string, id: string,                    before?: unknown, after?: unknown) => Promise<void>;    // Lecture   fetchLogs:      (filters?: AuditFilters) => Promise<void>;   exportToCSV:    () => void;    // Détection comportement suspect   checkSuspicious: (userId: number) => SuspiciousActivity[]; }  // Règles de détection de fraude // ≥ 5 remises en 1 heure par le même caissier → alerte // ≥ 3 annulations en 30 minutes → alerte // Mouvement stock > 3× la moyenne journalière → alerte 4. Pages & Composants Manquants — Implémentation 4.1 SuperAdminDashboard — /superadmin La page Super Admin actuelle d'App-Depot n'a qu'un dashboard CA consolidé. StockMan requiert une vue réseau SaaS complète. Widget à ajouter Contenu / Données Statut Carte MRR Revenu mensuel récurrent total en FCFA ❌ ABSENT Carte Tenants actifs / expirés Nb tenants par statut avec sparkline ❌ ABSENT Alertes réseau Licences expirant dans 7j, erreurs serveur critiques ❌ ABSENT Top 5 tenants (CA) Classement des clients les plus actifs ❌ ABSENT Carte CA consolidé Chiffre d'affaires de tous les tenants ⚠️ PARTIEL Logs réseau en direct Dernières 20 actions sur tous les tenants ❌ ABSENT Graphique MRR 12 mois Évolution du revenu récurrent ❌ ABSENT // src/pages/superadmin/SuperAdminDashboard.tsx — Structure export default function SuperAdminDashboard() {   const { allTenants } = useTenantStore();   const activeTenants  = allTenants.filter(t => t.status === 'active');   const expiredTenants = allTenants.filter(t => t.status === 'expired');   const mrr = activeTenants.reduce((sum, t) => sum + PLAN_LIMITS[t.plan].priceXAF, 0);    return (     <SuperAdminLayout>       {/* Ligne 1 — KPI Cards */}       <KPIGrid>         <KPICard label='MRR'           value={mrr}            unit='FCFA' trend='+12%' />         <KPICard label='Tenants actifs' value={activeTenants.length}       icon='users'  />         <KPICard label='Expirés'        value={expiredTenants.length}       icon='warning' color='red' />         <KPICard label='Modules déployés' value={enabledModules.length}    icon='puzzle'  />       </KPIGrid>        {/* Ligne 2 — Graphique + Alertes */}       <TwoColumnGrid>         <MRRChart data={mrrHistory} />         <NetworkAlertsPanel />       </TwoColumnGrid>        {/* Ligne 3 — Logs + Top clients */}       <TwoColumnGrid>         <RealtimeLogsPanel limit={20} />         <TopTenantsRanking tenants={allTenants} />       </TwoColumnGrid>     </SuperAdminLayout>   ); } 4.2 CreateTenantPage — Wizard 5 Étapes (/superadmin/tenants/new) Page absente. C'est le flux de création d'un nouveau client SaaS, conforme au §3.3 du CDC StockMan. // src/pages/superadmin/CreateTenantPage.tsx // Wizard à 5 étapes avec validation Zod à chaque étape  const STEPS = [   { id: 1, label: 'Informations',  component: StepCompanyInfo   },   { id: 2, label: 'Plan',          component: StepSelectPlan    },   { id: 3, label: 'Modules',       component: StepSelectModules },   { id: 4, label: 'Paramètres',    component: StepInitialParams },   { id: 5, label: 'Génération',    component: StepGenerate      }, ];  // Étape 1 — Champs obligatoires // name, commerceType, city, phone, email, rccm?, nui?  // Étape 2 — Sélection plan avec suggestion automatique // Si commerceType === 'boutique' → badge 'Basique recommandé' // Si commerceType === 'supermarche' → badge 'Pro/Premium recommandé'  // Étape 3 — Modules pré-cochés selon plan, modifiables // Core toujours coché et désactivé (obligatoire)  // Étape 4 — maxUsers, devise, tvaRate, logo, thème couleur  // Étape 5 — Génération automatique en < 30 secondes // • Clé licence : SM-2026-XXXX-XXXXXX // • Création schéma BDD isolé // • Email envoyé au client // • Affichage du bouton 'Accéder au tableau de bord' 4.3 TenantDetailPage — /superadmin/tenants/:id Vue 360° d'un client depuis le Super Admin. Absente dans App-Depot. Section Contenu Statut En-tête client Nom, plan, statut, expiration, nb users, last login ❌ ABSENT Modules actifs Toggle ON/OFF par module avec activation directe ❌ ABSENT Logs temps réel Dernières 50 actions du tenant avec filtres ❌ ABSENT Actions Modifier, Changer plan, Suspendre, Supprimer ❌ ABSENT Correction bug [Détail] [Corriger] [Envoyer patch] sur erreur ❌ ABSENT 4.4 ModulesPage — /superadmin/modules Gestion complète des modules plugins. Absente dans App-Depot. // src/pages/superadmin/ModulesPage.tsx — Fonctionnalités requises  // 1. Tableau de tous les modules installés //    Colonnes : Nom | Version | Tenants actifs | Statut | Actions  // 2. Zone de dépôt (Drag & Drop) pour importer un .zip //    → Validation manifest.json automatique //    → Vérification dépendances //    → Bouton [DÉPLOYER] si validation OK  // 3. Par module : [Info] [Logs] [Màj] [Désactiver globalement]  // Composants nécessaires : // <ModulesTable /> // <ModuleUploadZone /> // <ManifestValidator /> // <DeployConfirmModal />  // Validation manifest (règles CDC §2.3) : const REQUIRED_MANIFEST_FIELDS = [   'id', 'name', 'version', 'stockman_min',   'description', 'license_tiers', 'depends_on', 'permissions' ]; 4.5 PinLoginPage — /login/pin Accès rapide caissier par PIN 4-6 chiffres. Actuellement App-Depot n'a que email/mot de passe. // src/pages/auth/PinLoginPage.tsx  export default function PinLoginPage() {   const [pin, setPin] = useState('');   const [error, setError] = useState('');   const { loginWithPin } = useAuthStore();    // Clavier numérique 3x4 style caisse enregistreuse   // Chaque chiffre saisi → affichage de points (•••• style ATM)   // Validation automatique à 4 chiffres (ou 6 si configuré)   // Après 3 tentatives échouées → blocage 5 minutes    return (     <div className='min-h-screen flex items-center justify-center bg-gray-900'>       <PinCard>         <AvatarDisplay />          {/* Photo du caissier actif */}         <PinDots count={pin.length} max={6} />         <NumericKeypad onPress={handleKeyPress} />         <Link to='/login'>Connexion par mot de passe</Link>       </PinCard>     </div>   ); } 4.6 StockMovementsPage — /admin/stock/movements App-Depot décrémente le stock à la vente mais n'expose pas les 8 types de mouvements. Cette page est entièrement absente. Mouvement Description / Formulaire Sens Statut Vente Lié automatiquement au ticket PDV ➖ Sortie ✅ PRÉSENT Réception achat Lié au Bon de Réception validé ➕ Entrée ❌ ABSENT Retour client Avoir + remise en stock auto ➕ Entrée ❌ ABSENT Retour fournisseur BL retour + fournisseur + motif ➖ Sortie ❌ ABSENT Transfert dépôt Dépôt source → Dépôt destination ↔ Les deux ❌ ABSENT Ajustement inventaire Après session inventaire physique ↔ Les deux ❌ ABSENT Casse / Perte Déclaration + photo + responsable ➖ Sortie ❌ ABSENT Consommation interne Usage interne par département ➖ Sortie ❌ ABSENT 4.7 InventorySessionPage — /admin/stock/inventory Processus d'inventaire physique conforme au diagramme d'activité §6.4 du CDC. Entièrement absent d'App-Depot. // src/pages/admin/InventorySessionPage.tsx — 4 états  // ÉTAT 1 : Accueil — Créer une session // Formulaire : date, périmètre (tous / catégorie / rayon), responsables // Bouton : [Démarrer l'inventaire]  // ÉTAT 2 : En cours — Saisie des quantités // Liste des produits SANS les quantités théoriques (évite biais) // Pour chaque produit : [Scan QR] + champ quantité réelle // Barre de progression : X / Y produits comptés  // ÉTAT 3 : Analyse des écarts // Tableau : Produit | Théorique | Réel | Écart | Valeur FCFA // Tri par écart décroissant (les plus importants en premier) // Action par ligne : [Recompter] | [Déclarer perte] | [Valider]  // ÉTAT 4 : Clôture — Rapport PDF généré // Stocks mis à jour automatiquement // Rapport archivé + bouton téléchargement 4.8 ProductUnitsPage — /admin/catalogue/:id/units Gestion des 6 niveaux d'unités par produit. Absent d'App-Depot qui n'a qu'un seul prix de vente. // src/pages/admin/ProductUnitsPage.tsx  export default function ProductUnitsPage() {   // Interface de type 'grille' — chaque ligne = une unité   // Colonnes : Niveau | Nom unité | Facteur conversion | Prix vente | Code-barres | Actions    // Ligne 1 : Bol       | 0.25 × Kg | 250 FCFA  | [scan] | [✏] [🗑]   // Ligne 2 : Kg (BASE) | 1         | 900 FCFA  | [scan] | [✏] [🗑]  ← unité de base   // Ligne 3 : Sac 5kg   | 5 × Kg    | 4200 FCFA | [scan] | [✏] [🗑]   // ...    // Bouton [+ Ajouter une unité] → modal avec formulaire   // Bouton [Imprimer étiquettes] → génère QR codes pour impression    // ⚠️ Validation : facteur conversion doit être > 0   // ⚠️ Une seule unité peut être 'isDefault: true' } 4.9 CashClosurePage améliorée — /vendor/close App-Depot a une clôture simplifiée. StockMan requiert le processus en 7 étapes avec calcul d'écart et validation gérant. # Étape StockMan Ce qui manque dans App-Depot Statut 1 Caissier annonce fermeture Présent (bouton clôture) ✅ PRÉSENT 2 Solde théorique calculé auto Non calculé — App-Depot demande juste le montant réel ❌ ABSENT 3 Caissier compte physiquement Saisie montant cash mais sans détail billet/pièce ⚠️ PARTIEL 4 Calcul écart ± automatique Absent — pas de comparaison théorique vs réel ❌ ABSENT 5 Justification si écart Champ note basique mais sans logique d'écart ⚠️ PARTIEL 6 Validation gérant Absent — pas de workflow approbation Admin ❌ ABSENT 7 Archivage immuable + PDF PDF présent mais archivage non immuable ⚠️ PARTIEL 4.10 TenantSettingsPage — /admin/settings Page de personnalisation du tenant. Entièrement absente d'App-Depot. Section Paramètres Qui peut modifier Identité Logo, nom affiché, message d'accueil Admin Magasin Thème Couleur primaire, couleur accent, mode sombre Admin Magasin Fiscalité TVA (0% / 19,25% / Exonéré), RCCM, NUI Admin Magasin Langue Français / Anglais / Pidgin Admin Magasin Caisse Plafond remise par rôle, plafond crédit client Admin Magasin Notifications Seuils alertes stock, destinataires SMS/email Admin Magasin Verrouillable par Logo réseau, thème, branding Super Admin uniquement 5. Composants UI Réutilisables à Créer Ces composants sont transversaux — ils seront utilisés dans plusieurs pages. Ils doivent être créés dans /src/components/ et respecter le design system Shadcn/Tailwind existant. Composant Usage / Props clés Priorité CDC ── Auth & Sécurité    <NumericKeypad /> Pavé numérique pour saisie PIN. onPress, onDelete, onConfirm 🔴 CRITIQUE v0.1 <PinDots /> Affichage ●●●● du PIN en cours. count, max, error 🔴 CRITIQUE v0.1 <PermissionGuard /> HOC/wrapper: <PermissionGuard action='vente.cancel'>. Masque si interdit 🔴 CRITIQUE v0.1 <RoleBadge /> Badge coloré par rôle. role: 'caissier' | 'admin'... 🟠 HAUTE v0.1 ── SaaS & Tenant    <TenantCard /> Carte résumé d'un tenant. tenant, onSelect, onSuspend 🔴 CRITIQUE v0.7 <PlanBadge /> Badge coloré par plan. plan: 'basique'|'pro'|'premium'|'reseau' 🔴 CRITIQUE v0.7 <LicenceKey /> Affichage formaté de la clé SM-2026-XXXX-XXXXXX avec copie 🟠 HAUTE v0.7 <PlanSelector /> Cartes comparatives des 4 plans avec prix et modules inclus 🔴 CRITIQUE v0.7 <ModuleToggle /> Switch ON/OFF pour activer un module. moduleId, tenantId, disabled 🔴 CRITIQUE v0.7 <MRRCard /> Carte KPI MRR avec sparkline. value, trend, currency='XAF' 🟠 HAUTE v0.7 <RealtimeLogsPanel /> Feed de logs avec auto-scroll. limit, tenantId?, filters 🟠 HAUTE v0.7 <TenantWizard /> Wizard multi-étapes création client. steps, onComplete 🔴 CRITIQUE v0.7 ── Catalogue & Stock    <UnitLevelGrid /> Grille éditable des 6 niveaux d'unités. productId, onSave 🔴 CRITIQUE v0.2 <UnitSelector /> Dropdown sélection unité en PDV. productId, onSelect, showPrice 🔴 CRITIQUE v0.3 <QRScanner /> Composant caméra html5-qrcode. onScan, onError, facingMode 🔴 CRITIQUE v0.3 <QRGenerator /> Génère et affiche QR. value, size, downloadable 🟠 HAUTE v0.3 <StockAlertBadge /> Badge couleur selon niveau stock. ROUGE/ORANGE/JAUNE/BLEU 🟠 HAUTE v0.2 <MovementTypeIcon /> Icône + couleur par type mouvement. type: MovementType 🟠 HAUTE v0.2 <InventoryCountRow /> Ligne saisie comptage physique. product, onCountChange 🟠 HAUTE v0.2 ── Caisse & PDV    <ClosureVarianceCard /> Affiche écart caisse coloré. theoretical, actual, variance 🟠 HAUTE v0.3 <PaymentSummaryTable /> Récap par mode paiement. cash, mtn, orange, virement 🟠 HAUTE v0.3 <DiscountInput /> Champ remise avec validation plafond. max, value, onChange 🟠 HAUTE v0.3 <SuspiciousActivityAlert /> Alerte comportement suspect. userId, type, count 🟠 HAUTE v0.1 ── Layout    <SuperAdminLayout /> Layout dédié Super Admin. Sidebar, header, breadcrumb 🔴 CRITIQUE v0.7 <ModuleGatedSection /> Affiche contenu seulement si module activé. moduleId, fallback 🔴 CRITIQUE v0.7 <OfflineBanner /> Bandeau orange si hors ligne. syncing, pendingCount ⚠️ PARTIEL v0.1 <SyncStatusIndicator /> Indicateur sync en temps réel. pendingOps, lastSync 🟠 HAUTE v0.1 <ThemeProvider /> Applique le thème du tenant courant. theme: TenantTheme 🟠 HAUTE v0.7 6. Éléments de Sécurité Frontend Manquants 6.1 Renforcement de l'Authentification Élément sécurité Implémentation requise Statut JWT RS256 + refresh token Token 15min + refresh 7j — stocker en mémoire, jamais localStorage ❌ ABSENT PIN 4-6 chiffres (caisse) Hash PIN côté client avant envoi — bcrypt ou argon2 wasm ❌ ABSENT Blocage après 3 tentatives PIN Compteur local + horloge 5min — stocker dans sessionStorage ❌ ABSENT Validation Zod client + serveur Tout formulaire validé par schéma Zod avant soumission ⚠️ PARTIEL Route guards par permission <PermissionGuard action='...'> sur chaque section sensible ❌ ABSENT Session timeout automatique Inactivité 30min → retour PIN login (configurable) ❌ ABSENT 6.2 Audit Frontend — Ce qui doit être loggué // Chaque action critique doit appeler logAudit() // Voici la liste complète des actions à instrumenter :  // ── Authentification   'user.login'              // connexion réussie   'user.login_failed'       // tentative échouée   'user.logout'             // déconnexion   'user.pin_login'          // ouverture session caisse par PIN  // ── Ventes   'sale.create'             // vente validée   'sale.cancel'             // annulation vente   'sale.discount_applied'   // remise appliquée (montant, %)   'sale.credit_granted'     // vente à crédit  // ── Stock   'stock.movement_created'  // tout mouvement (type + qté avant/après)   'stock.inventory_started' // début session inventaire   'stock.inventory_closed'  // clôture inventaire   'stock.adjustment'        // ajustement manuel justifié  // ── Caisse   'caisse.opened'           // ouverture session caisse   'caisse.closed'           // clôture initiée   'caisse.validated'        // validation gérant   'caisse.rejected'         // rejet gérant (motif)  // ── Admin   'user.created'            // création compte   'user.role_changed'       // changement de rôle   'user.suspended'          // suspension   'product.price_changed'   // modification prix   'tenant.plan_changed'     // changement plan SaaS 7. Plan d'Implémentation Recommandé Voici l'ordre de développement optimal. Chaque sprint dure environ 2 semaines. Sprint Objectif Livrables Durée S1 Fondations Core usePermissionStore + PermissionGuard + PIN Login + AuditStore + schéma Dexie étendu 2 semaines S2 SaaS Multi-Tenant useTenantStore + useLicenceStore + SuperAdminLayout + TenantCard + PlanSelector + PlanBadge 2 semaines S3 Super Admin Pages SuperAdminDashboard (complet) + TenantsListPage + CreateTenantPage (wizard) 2 semaines S4 Super Admin Suite TenantDetailPage + ModulesPage + LicencesPage + LogsPage + ThemesPage 2 semaines S5 Catalogue Multi-Unités useProductUnitsStore + UnitLevelGrid + UnitSelector + ProductUnitsPage + QRScanner 2 semaines S6 Stock Complet useStockMovementsStore + StockMovementsPage + InventorySessionPage + 8 formulaires mouvements 2 semaines S7 Caisse Professionnelle useCashSessionStore + PinLoginPage + CashClosurePage (7 étapes) + workflow validation gérant 2 semaines S8 Finalisation & Tests TenantSettingsPage + ThemeProvider + tests E2E Playwright + intégration complète + déploiement 2 semaines TOTAL 8 sprints — Conformité v0.1 → v0.7 18 pages · 8 stores · 12 modèles · 35+ composants 16 semaines 🚀 Point de départ recommandé : Commencez par le Sprint 1 (Fondations). usePermissionStore et le schéma Dexie étendu sont des prérequis pour TOUS les autres sprints. Sans eux, rien ne peut être construit proprement. © 2026 RootRise Group  ·  Guide Frontend StockMan v0.7  ·  Document confidentiel
